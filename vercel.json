{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "pnpm --filter @fomora/db db:generate && cd apps/web && pnpm vercel-build",
  "outputDirectory": "apps/web/.next",
  "functions": {
    "apps/web/app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
