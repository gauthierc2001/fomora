{
  "version": 2,
  "installCommand": "pnpm install --no-frozen-lockfile",
  "buildCommand": "cd packages/db && pnpm db:generate && cd ../../apps/web && next build",
  "outputDirectory": "apps/web/.next",
  "functions": {
    "apps/web/app/api/**/*.ts": {
      "maxDuration": 10
    }
  }
}
